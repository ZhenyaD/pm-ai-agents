---
description: Create SRS in Confluence from artifacts or my input, matching my example format
---
Use the ba-agent.

Parse $ARGUMENTS for the target workspace (default test) and the
source (UI flow, PRD, website URLs, or a free-text description).

Follow .claude/knowledge/ba/srs-rules.md and study the screenshots in
.claude/knowledge/ba/srs-examples/ — match their structure, fields, and
description style exactly.

Steps:
1. Derive an SRS structure and description.
2. Show me the draft of a structure for review (do NOT create anything yet).
3. On approval, confirm the Confluence space key then create the SRS in the
   target workspace and return the issue keys/links.